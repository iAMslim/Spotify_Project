import { AxiosInstance } from 'axios';
interface SpotifyOptions {
    token: string;
    refreshToken?: string;
    refreshTokenFunction?: (refreshToken?: string) => Promise<string>;
}
export declare const init: (config: SpotifyOptions) => void;
export declare const getToken: () => string;
export declare const getAxiosSpotifyInstance: () => AxiosInstance;
export {};
